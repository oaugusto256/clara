{
  "name": "@clara/rules-engine",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@clara/schemas": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check",
    "format": "biome format \"src/**/*.{ts,tsx,js,json,md}\"",
    "test": "vitest",
    "test:ci": "vitest run"
  }
}
